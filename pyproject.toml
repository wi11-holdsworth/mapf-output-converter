[tool.poetry]
name = "converter"
version = "0.1.0"
description = ""
authors = ["Will Holdsworth <will.holdsworth@monash.edu>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
